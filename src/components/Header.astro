---
const nav = [
  { label: 'Recipes', href: '/recipes' },
  { label: 'Brew Guides', href: '/guides' },
  { label: 'Learn', href: '/learn' },
  { label: 'Journal', href: '/journal' },
  { label: 'Shop', href: '/shop' },
];
const { pathname } = Astro.url;
---

<header class="sticky top-0 z-40 border-b border-line bg-paper/95 backdrop-blur">
  <div class="mx-auto flex max-w-6xl items-center gap-6 px-5 py-3.5">
    <a href="/" class="font-display text-lg font-semibold tracking-[0.08em] whitespace-nowrap">
      THE DAILY BREW
    </a>

    <nav class="hidden flex-1 justify-center gap-6 text-sm text-ink-soft lg:flex" aria-label="Main">
      {nav.map((item) => (
        <a
          href={item.href}
          class:list={[
            'transition-colors hover:text-ink',
            pathname.startsWith(item.href) && 'font-medium text-ink',
          ]}
        >
          {item.label}
        </a>
      ))}
    </nav>

    <form action="/recipes" method="get" class="hidden items-center md:flex" role="search">
      <label class="flex items-center gap-2 rounded-full border border-line bg-card px-3.5 py-1.5 text-sm text-ink-soft focus-within:border-clay">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <circle cx="11" cy="11" r="7" />
          <path d="m20 20-3.5-3.5" />
        </svg>
        <input
          type="search"
          name="q"
          placeholder="Search recipes & guides"
          class="w-40 bg-transparent outline-none placeholder:text-ink-soft/70"
        />
      </label>
    </form>

    <a
      href="/recipes"
      class="ml-auto rounded-md bg-clay px-4 py-2 text-sm font-medium text-white transition-colors hover:bg-clay-dark md:ml-0"
    >
      Explore recipes
    </a>
  </div>
</header>
